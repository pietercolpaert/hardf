language: php
php:
  - '7.2'
  - '7.3'
  - '7.4'
  - nightly

matrix:
    fast_finish: true
    allow_failures:
        - php: nightly

sudo: false
env:
before_script:
  - composer install

script:
  - composer phpstan
  - composer phpunit-with-coverage
