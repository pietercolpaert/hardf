language: php

dist: xenial

os: linux

php:
  - '7.2'
  - '7.3'
  - '7.4'
  - nightly

jobs:
    fast_finish: true
    allow_failures:
        - php: nightly

before_script:
  - composer install

script:
  - composer phpstan
  - composer phpunit-with-coverage
